query GetProductById($getProductByIdId: String!) {
  product(id: $getProductByIdId) {
    brand
    category {
      id
      name
    }
    description
    discount
    id
    name
    price
    stock
    store {
      ...store
    }
    thumbnails {
      alt
      id
      src
      type
    }
  }
}